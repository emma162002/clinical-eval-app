services:
  app:
    build: .
    container_name: clinical-eval-app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - APP_ENV=production
    restart: unless-stopped
