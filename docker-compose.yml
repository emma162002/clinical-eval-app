services:
  app:
    build: .
    container_name: clinical-eval-app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/code/data
    environment:
      - APP_ENV=production
      - SECRET_KEY=replace-this-with-a-random-secret-before-sharing
    restart: unless-stopped
